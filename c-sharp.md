See below:
```cs
Console.WriteLine(1.623f);
Console.WriteLine(12.39816m);

Console.WriteLine(true);

char userOption;

int gameScore;

decimal particlesPerMillion;

bool didProcessCustomerRequest;

string firstName;
firstName = "Jon";

Console.Write(firstName);

// var message; // not allowed, declaration must be explicit if there is no assignment
var message = "Sup, girl.";
```
